const famous_person: string = 'VINCENT VAN GOGH';
const quote: string = 'I thought i would be understood without words';
const msg: string = `${famous_person} once said, "${quote}"`;

console.log(msg);
